# VG19 gates: the slope is real, converges cleanly, and does not explain `kappa`

> [!NOTE]
> Drafted by an LLM-based AI tool (Claude Code/Opus 5).

> [!IMPORTANT]
> Gate results, 2026-08-21. G2, G4 and G5 of [202608141900](202608141900-child-slope-implementation-plan.md) are complete; **G3 (recovery) is running** and G5b (the high-age leverage check) is not started. The headline is a **split verdict**: the child slope is decisively evidenced and predicts better than VG10 on both outcomes, but it does **not** shrink `kappa_u`'s decline — which was, in the plan's own words, "the whole reason for the exercise". Both halves matter and neither cancels the other.

## 1. G5 — convergence at `rep`. Clean pass

| check       | value                    | threshold |
| ----------- | ------------------------ | --------- |
| max R-hat   | 1.0092                   | ≤ 1.01    |
| min ESS     | 822                      | ≥ 400     |
| divergences | **0**                    | 0         |
| BFMI        | 0.513-0.556 (all chains) | ≥ 0.3     |

`rhat_failing` and `ess_failing` are both empty and no `CONVERGENCE_CAVEATS.txt` was written. This is a **cleaner fit than any of the three VG13 window fits**, all of which fail BFMI. 37m 23s at `rep`, against a budget of 1.5-2x VG10's hour.

**The named risk did not materialise.** The plan's G5 risk was a funnel on `tau1`, since 433 of 767 children contribute a single production observation. `tau_subj_u_1` came back with ESS 862 and R-hat 1.008 — unremarkable. Non-centring plus a prior with mass near zero was sufficient; `rep-hightune` was not needed.

## 2. The fitted structure, and its stability

89% equal-tailed intervals, `rep`. The `dev` column is a 2-chain 10-minute fit and is shown only to make the point that these values are not tier-sensitive:

| parameter                       |    dev |    rep | 89% ETI              |
| ------------------------------- | -----: | -----: | -------------------- |
| `tau_subj_u_0` (spread at 36mo) |  0.752 |  0.751 | [0.696, 0.807]       |
| `tau_subj_u_1` (rate, per year) |  0.185 |  0.176 | [0.092, 0.242]       |
| `tau_subj_u_rho`                | −0.218 | −0.219 | [−0.423, **−0.008**] |
| `tau_subj_q_0`                  |  1.207 |  1.207 | [1.121, 1.298]       |
| `tau_subj_q_1`                  |  0.634 |  0.640 | [0.538, 0.743]       |
| `tau_subj_q_rho`                |  0.473 |  0.469 | [0.338, 0.591]       |

**Both rates exclude zero**, so the model of record's nesting at `tau1 = 0` is rejected on both outcomes. `tau_subj_q_rho` = 0.469 [0.338, 0.591] **contains Gate 1's maximum-likelihood value of +0.43**, which is the cross-check the plan asked for, passing.

Two cautions. `tau_subj_u_rho`'s upper bound is **−0.008** — it excludes zero by a hair, so the qualitative claim it licenses (comprehension spread is _tightest_ near 47 months and fans out on both sides, since the parabola's minimum sits at `D = −rho01 tau0 / tau1`) is real but marginal, and should not be leant on. And `tau_subj_q_1` = 0.64/year is well above Gate 1's 0.29/year measured on the spoken proportion — not a contradiction, because `q` is the conversion ratio and not the level, but the two are not interchangeable and should not be quoted as though they were.

`tau0` fell on both outcomes against VG20's constant scales (0.751 vs 0.787; 1.207 vs 1.286), which is the rate taking up spread the constant offset previously had to carry.

## 3. G4 — LOO against VG10. VG19 wins on both outcomes

Paired differences, computed pointwise and dropping the same degenerate observations the engine's own `_loo_dropping_degenerate` drops:

| outcome    | VG19 elpd | VG10 elpd | paired diff |   SE |          |
| ---------- | --------: | --------: | ----------: | ---: | -------- |
| spoken     |   −6278.7 |   −6410.9 |  **+132.1** | 17.2 | +7.69 SE |
| understood |   −5446.5 |   −5463.0 |   **+16.5** |  4.5 | +3.70 SE |

The elpd values reproduce the engine's own persisted `loo_summary.csv` to the digit, which is what verifies the replication rather than the agreement being assumed.

**The reliability caveat is load-bearing and cuts against the winner.** `p_loo` is 744.8 on 1416 spoken observations — more than half the data points as effective parameters — and 399 of them (28%) have Pareto k > 0.7. VG10 has 298 (21%). **The model that wins is the one whose PSIS approximation is more strained**, which is the pattern in which LOO is least trustworthy. The direction is consistent across both outcomes and the margins are large, but a claim that VG19 _predicts better_ should be settled by `kfold_loso.py`, not by this table. Recorded as: LOO favours VG19, reliability contested, k-fold outstanding.

## 4. G4's headline diagnostic — refuted

The plan: _"does `kappa_u`'s 24→48 month decline shrink once the child slope can absorb drift? That number is the whole reason for the exercise."_

Posterior median `kappa_u` on the plot grid:

| fit              |     24 mo |     48 mo | ratio 48/24 |
| ---------------- | --------: | --------: | ----------: |
| VG10 (parent)    |     64.82 |     24.41 |       0.377 |
| VG20 (record)    |     61.58 |     24.68 |       0.401 |
| **VG19 (slope)** | **73.39** | **24.36** |   **0.332** |

**It steepened.** The decline is _larger_ under the slope, not smaller.

This was checked for the obvious confound before being believed: `kappa_u` and `kappa_s` are byte-identical across all three definitions, all three were fitted 19-21 August under the promoted `kappa-anchor-18-72-floor` block, and all three carry `n_trials = 810`. The difference is real.

The mechanism is coherent rather than noise, which is what makes it informative. `kappa_u` **rose** at 24 months (73.4 against VG10's 64.8 — less residual dispersion, so the slope did absorb variation there) and is unchanged at 48 (24.36 against 24.41). That is what the algebra predicts: with `rho01` = −0.219 and a reference age of 36 months, the slope _adds_ between-child variance at 24 months (2·rho·tau0·tau1·D + tau1²D² = +0.089 at D = −1) and slightly _removes_ it at 48 (−0.027 at D = +1). The child slope explains dispersion where it was asked to and not where the decline lives.

**So `kappa_u`'s decline is not unmodelled child drift.** The plan anticipated an informative negative here and this is one. It removes the leading candidate explanation and sends [#229](https://github.com/dseinternational/vocabulary-growth/issues/229) and the mechanism hunt (#49) elsewhere.

## 5. What this does and does not license

It **does** establish that children differ in their own rate of change, on both outcomes, in a model that converges cleanly and is nested at the model of record. It **does not** establish that VG19 should replace anything: VG19 was gated against VG10, not VG20, and by [202608211500](202608211500-vg19-registration.md) §2 the two are not composable — VG19 sets `rho_uq` to zero, which VG20 estimates at +0.368 with an interval excluding zero. A model of record carrying the slope is the 4x4 that neither model is, and it needs its own Gate 1.

Outstanding: **G3** (recovery at `test`, running), **G5b** (the high-age leverage refit, capped at 84 months), and k-fold if the LOO claim is to be published.
