---
title: "Adding signing (VG14 → VG15)"
abstract: "*This chapter narrates the extension of the joint model to signing: from VG14, which adds a signed production ratio but assumes signing and speaking are independent given age, to VG15, which estimates the within-understood sign–speech association and so replaces the independence-based upper bound on total expressive vocabulary with a data-identified estimate.*"
---

::: {.callout-note}
Drafted by an LLM-based AI tool (Claude Code/Opus 4.8).
:::

<!--
PURPOSE: the second development arc. Cite @sec-ratios and @sec-association; do not
re-derive. Per-step diagnostics (incl. the four-cell PPC and psi prior/posterior)
live in Appendix B3. Supersession claim carries its model-comparison statistic
inline (@sec-supersedes).
-->

## From an independence bound to a data-identified total {#sec-lineage-signing}

**TODO**: orientation — signing is DS-only (@sec-signing-data). Both models share the engine; the change is how the sign/speech overlap is treated.

| Step | Added structure | Problem it addressed | Out-of-sample effect (LOO/ELPD) |
|---|---|---|---|
| **VG14** | Third (signed) production ratio $r(a)$ (@sec-ratios); $p_{\text{any}}$ as an **independence upper bound** | Bring signing into the joint model | — (baseline) |
| **VG15** | Plackett association $\psi$ + four-cell likelihood (@sec-association); study **and** subject REs; VG10 stabilisation | Independence assumption is not data-identified; signed peak study-confounded | **TODO** |

: The signing development lineage. Full per-step priors and diagnostics in Appendix B3. {#tbl-lineage-signing}

**TODO**: prose for each step, with the inline ELPD difference (± SE) and the named structural issue.

## Supersession and scope

**TODO**: plain-language statement that VG15 supersedes VG14, and the prominent caveat — $\psi$ rests on ~60 uk\_02 four-cell rows (~34 children) and is descriptive/exploratory. Reuse the transcluded signing-caveat note shared with the signing findings chapter. Link to Appendix A.
